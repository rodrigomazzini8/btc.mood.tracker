# ==========================================================================
# requirements do Hugging Face Space — INCLUI o FinBERT (torch/transformers).
# Diferente do requirements.txt da raiz (que é leve, para o Streamlit Cloud),
# aqui o Space tem RAM suficiente para a IA pesada.
# ==========================================================================

# --- Núcleo (dados, gráficos, rede) ---
requests>=2.28
pandas>=1.5
numpy>=1.23
matplotlib>=3.6

# --- Sentimento simples ---
vaderSentiment>=3.3.2

# --- Atenção do público (opcional) ---
pytrends>=4.9

# --- Dashboard ---
streamlit>=1.28
plotly>=5.15

# --- IA avançada: FinBERT ---
transformers>=4.30
torch>=2.0
