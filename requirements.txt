# ==========================================================================
# btc-mood-tracker — dependências
# Todas as fontes de dados são gratuitas e SEM chave de API.
# ==========================================================================

# --- Núcleo (dados, gráficos, rede) ---
requests>=2.28
pandas>=1.5
numpy>=1.23
matplotlib>=3.6

# --- Análise de sentimento simples (didática) ---
vaderSentiment>=3.3.2

# --- Atenção do público (opcional; trata rate limit sem quebrar) ---
pytrends>=4.9

# --- Dashboard interativo ---
streamlit>=1.28
plotly>=5.15

# --- Análise de sentimento avançada (FinBERT) ---
# Pesados: só necessários para o script 05 e para o toggle FinBERT do dashboard.
# Se quiser apenas o básico (scripts 02-04 + dashboard sem IA pesada),
# pode comentar as três linhas abaixo.
transformers>=4.30
torch>=2.0
