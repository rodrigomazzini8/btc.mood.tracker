# ==========================================================================
# btc-mood-tracker — dependências (deploy-friendly)
# Todas as fontes de dados são gratuitas e SEM chave de API.
#
# Este arquivo é LEVE de propósito, para rodar no Streamlit Community Cloud
# (tier grátis, ~1 GB de RAM). O FinBERT (torch + transformers) é pesado e
# fica em requirements-finbert.txt — instale-o só localmente, se quiser a IA
# avançada. O dashboard cai automaticamente para o VADER quando o FinBERT
# não está instalado.
# ==========================================================================

# --- Núcleo (dados, gráficos, rede) ---
requests>=2.28
pandas>=1.5
numpy>=1.23
matplotlib>=3.6

# --- Análise de sentimento simples (didática) ---
vaderSentiment>=3.3.2

# --- Atenção do público (opcional; trata rate limit sem quebrar) ---
pytrends>=4.9

# --- Dashboard interativo ---
streamlit>=1.28
plotly>=5.15
