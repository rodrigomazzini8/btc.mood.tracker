# ==========================================================================
# btc-mood-tracker — dependências (versão LEVE, pronta para deploy)
# Todas as fontes de dados são gratuitas e SEM chave de API.
#
# Esta lista é proposital­mente leve para caber no Streamlit Community Cloud
# (tier grátis). O FinBERT (torch + transformers) é PESADO e foi separado em
# requirements-finbert.txt — instale-o só localmente, se quiser a IA avançada.
# Sem ele, o dashboard cai automaticamente para o VADER.
# ==========================================================================

# --- Núcleo (dados, gráficos, rede) ---
requests>=2.28
pandas>=1.5
numpy>=1.23
matplotlib>=3.6

# --- Análise de sentimento simples (didática) ---
vaderSentiment>=3.3.2

# --- Atenção do público (opcional; trata rate limit sem quebrar) ---
pytrends>=4.9

# --- Dashboard interativo ---
streamlit>=1.28
plotly>=5.15
