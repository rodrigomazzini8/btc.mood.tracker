# Configuração do Streamlit — tema escuro combinando com os gráficos.
# Sem segredos aqui: o projeto não usa nenhuma chave de API.

[theme]
base = "dark"
primaryColor = "#f7931a"            # laranja do Bitcoin
backgroundColor = "#0e1117"
secondaryBackgroundColor = "#161a25"
textColor = "#fafafa"

[browser]
gatherUsageStats = false
