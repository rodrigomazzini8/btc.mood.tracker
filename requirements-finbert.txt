# ==========================================================================
# Dependências PESADAS do FinBERT (IA avançada) — OPCIONAIS.
#
# Use SOMENTE localmente (ou num host com bastante RAM, ex.: Hugging Face
# Spaces). NÃO recomendado no Streamlit Community Cloud (tier grátis), pois
# o torch é grande demais para o limite de memória/tempo de build.
#
# Instalação:
#   pip install -r requirements.txt          # base (sempre)
#   pip install -r requirements-finbert.txt  # + FinBERT (opcional)
#
# Sem estes pacotes, o script 05 avisa e sai, e o toggle FinBERT do
# dashboard cai automaticamente para o VADER.
# ==========================================================================

transformers>=4.30
torch>=2.0
